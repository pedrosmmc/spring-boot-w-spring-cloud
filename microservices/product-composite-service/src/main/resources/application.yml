server.port: 7010
server.error.include-message: always

app:
  product-service:
    host: localhost
    port: 7001
  recommendation-service:
    host: localhost
    port: 7002
  review-service:
    host: localhost
    port: 7003

logging:
  level:
    root: INFO
    com.pedrocoelho: DEBUG

---
spring.config.activate.on-profile: docker

server.port: 8080

# INFO: The hostnames are used in the docker-compose.yml to make them accessible and found within the same conteinerized network
app:
  product-service:
    host: product
    port: 8080
  recommendation-service:
    host: recommendation
    port: 8080
  review-service:
    host: review
    port: 8080